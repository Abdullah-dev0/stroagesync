{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "dev": {
      "env": [
        "BETTER_AUTH_SECRET",
        "BETTER_AUTH_URL",
        "CLIENT_ORIGIN",
        "DATABASE_URL",
        "PORT"
      ]
    }
  }
}
